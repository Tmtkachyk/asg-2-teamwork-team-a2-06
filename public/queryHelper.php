<!-- <?php
function isValidStandardQuery()
{
  $title = $_GET["title"];
  return queryHasOneKey() && ($userid) && isStandardUser($userid);
}

function queryHasOneKey()
{
  return count($_GET) == 1;
}

function useridIsNumber($userid)
{
  return is_numeric($userid);
} -->
